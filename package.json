{
  "name": "codex-accounts",
  "version": "0.2.1",
  "description": "Manage multiple OpenAI Codex and Claude Code accounts",
  "type": "module",
  "files": [
    "dist"
  ],
  "bin": {
    "cx": "dist/index.js",
    "codex-accounts": "dist/index.js"
  },
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "test": "npm run build && node --test test/*.test.mjs",
    "start": "node dist/index.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {},
  "devDependencies": {
    "typescript": "^5.7.0",
    "@types/node": "^22.0.0"
  },
  "license": "MIT"
}
